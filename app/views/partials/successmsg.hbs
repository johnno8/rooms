{{#if messages}}
  <div class="ui positive message">
    <i class="close icon"></i>
    <div class="header">
      Success
    </div>
    {{#each messages}}
      <p>{{ message }}</p>
    {{/each}}
  </div>
{{/if}}